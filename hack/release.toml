# commit to be tagged for the new release
commit = "HEAD"

project_name = "image-service"
github_repo = "siderolabs/image-service"
match_deps = "^github.com/(siderolabs/[a-zA-Z0-9-]+)$"

# previous = -
# pre_release = true

# [notes]
